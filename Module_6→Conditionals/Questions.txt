Q.1)→ Display all the records from the orders table along with the cumulative sum and cumulative average of order_amount,
      ordered by order_date.
Q.2)→ Display all the records from the orders table along with the cumulative sum of order_amount from the first row to 
      the current row based on order_date. Explicitly specify the window frame.
Q.3)→ Display all the records from the orders table along with the total sum of order_amount for all rows, repeated on 
      every row, while ordering the data by order_date.
Q.4)→ Display the order_amount along with its ROW_NUMBER, RANK, and DENSE_RANK based on order_amount in ascending order.
